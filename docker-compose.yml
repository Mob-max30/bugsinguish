version: '3.8'

services:
  # frontend:
  #   build:
  #     context: ./frontend
  #   ports:
  #     - "5173:5173"
  #   environment:
  #     - VITE_API_BASE_URL=http://localhost:8080
  #   volumes:
  #     - ./frontend:/app

  # backend:
  #   build:
  #     context: ./backend
  #   ports:
  #     - "8080:8080"
  #   environment:
  #     - PORT=8080
  #     - DATABASE_URL=postgres://user:password@localhost:5432/bugsinguish?sslmode=disable
  #   volumes:
  #     - ./backend:/app
